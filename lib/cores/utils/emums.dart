/// image type enum
enum ImageTypes { network, file, asset, none }

/// snack bar type enum
enum SnackBarType { success, error, warning }

/// payment type enum
enum PaymentType { card, cash, transfer, none }

/// order status
enum OrderStatusEunm { pending, accepted, processing, enroute,  completed, Null }

